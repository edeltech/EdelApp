SET(TEST_LIBRARIES ${QT_QTTEST_LIBRARY} ${QT_LIBRARIES} ${EDEL_CURRENT_LIBRARY})

SUBDIRLIST(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})
FOREACH(subdir ${SUBDIRS})
    ADD_SUBDIRECTORY(${subdir})
ENDFOREACH()
